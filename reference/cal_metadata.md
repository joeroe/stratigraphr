# Extract metadata from a calibrated date

Extract metadata from a calibrated date

## Usage

``` r
cal_metadata(x)
```

## Arguments

- x:

  A `cal` object. See [`cal()`](cal.md).

## Value

A named list of metadata attributes.

## See also

Other functions for working with `cal` objects:
[`as.CalDates.cal()`](as.CalDates.cal.md), [`as_cal()`](as_cal.md),
[`cal()`](cal.md)
